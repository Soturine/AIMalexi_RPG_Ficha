/* ═══════════════════════════════════════════════════════════════════════════
   AIMalexi RPG · js/vendor/supabase.js
   SLOT DE VENDOR — biblioteca local (offline-first proíbe import de CDN em runtime).

   SUBSTITUIR pelo build ESM de @supabase/supabase-js no M5 (sync). Placeholder
   no M0.5 — sync ainda não plugado.

   Origem sugerida (baixar e commitar localmente):
     https://cdn.jsdelivr.net/npm/@supabase/supabase-js/+esm
   ═══════════════════════════════════════════════════════════════════════════ */

/* (vazio — vendor pendente para o M5) */
